name: simple
on:
  master
jobs:
  build:
    runs-on:
      - self-hosted
      - label-1
    steps:
      - uses: actions/checkout@v2
      - name: Run a one-line script
        run: |
          Echo Hello, World!
          Sleep 15
          Echo Goodbye!
        
      - name: Run a multi-line script
        run: |
          echo "${{github.workspace}}"
